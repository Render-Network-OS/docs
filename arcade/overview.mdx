---
title: "Arcade Overview"
description: "The High-Voltage Playground"
---

# The 555 Arcade

The **555 Arcade** is the user-facing layer of the ecosystem. It is where the protocol becomes visible. Built as a **Next.js 15 monorepo** (555-mono) with React 19, Turbopack, and Bun.

## Integration Architecture

The Arcade is not just a website; it is a **VAP Client**. Every game hosted in the arcade is wrapped in a Harness that communicates with the protocol.

### The Game Harness
The Harness is a React component that manages the VAP session lifecycle.

```tsx
<GameHarness appId="sector-13" onSessionStart={handleStart}>
  <PhaserGame />
</GameHarness>
```

### The Event Loop
1.  **Initialization**: The Harness connects to the VAP WebSocket.
2.  **Gameplay**: The user plays the game. The game engine emits events (`SCORE_UPDATE`, `ENEMY_KILL`).
3.  **Hashing**: The Harness aggregates events into a Merkle Root.
4.  **Signing**: The user's wallet (via session key) signs the root.
5.  **Transmission**: The signed payload is sent to the VAP Verifier.

### Authentication
Users connect via **SIWS** (Sign-In With Solana) using the Solana wallet adapter. Supports Phantom, Backpack, Solflare, and other major wallets.

## The Economy

The Arcade is the primary engagement surface for the ecosystem's reward system.

### Points (The Reward)
*   **Earning**: 10,000 points = 1 USDC. Earned through verified gameplay.
*   **Daily Bonus**: +100 points per game per wallet per CST day.
*   **Velocity**: Designed for high velocity — earned fast through engagement.

### Credits (The Settlement Unit)
*   Points convert to credits when ready for settlement.
*   Weekly settlement engine distributes USDC payouts.
*   Users can also self-withdraw at any time.

### $555 Token
*   Used for staking (VAP v2), burns, and protocol participation.
*   Cross-chain via sw4p: Solana, Base, Polygon.
*   Structural deflation via 20% buyback-and-burn from platform revenue.

## 18 Games

The Arcade hosts 18 games — 6 production titles and 12 beta games — spanning shooters, platformers, racing, RPGs, puzzles, and more. See the [Games Library](/arcade/games) for the full catalog.

### Score Normalization
Each game has a per-game normalization factor that maps raw scores to a comparable scale. This enables a unified cross-game leaderboard.

### Beta Mode
Beta games apply a 10% score scaling with an escalating penalty for repeat plays (1/50 → 1/500 → 1/5000 → 0). This incentivizes exploration across the full catalog.

## Leaderboards

Leaderboards are not just database rows; they are **Aggregated Proofs**.
*   **Source of Truth**: Redis ledger maintained by the VAP Verifier.
*   **Sync**: Synced to PostgreSQL for querying by the frontend.
*   **Verification**: Any score can be traced back to a signed VAP packet.
*   **Periods**: Day, Week, Month, Year, All-Time.
*   **Cross-Game**: Normalized rankings across all 18 games.
