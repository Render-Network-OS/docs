---
title: WP2.4 closure handover, corrigendum after verified-state audit
date: 2026-05-18
supersedes: docs/superpowers/audits/2026-05-18-wp2.4-closure-handover.md (commit 3dc34649)
status: corrigendum, real state of the closure queue after live verification
audit_session: 2026-05-18 receiving-team session
verified_via:
  - gh pr view across {234, 239, 247, 218, 222, 233} repo Render-Network-OS/sw4p-pro
  - git ls-remote and git fetch on origin Render-Network-OS/sw4p-pro
  - git log on staging/devnet-frontier-2026-05-16 and staging/devnet-frontier-rebased-2026-05-17
  - Read of sw4p-backend/contracts/registry/mainnet.json, deploy_targets_mainnet.json, deployed_addresses_v41.json at origin/master
  - Solana mainnet JSON-RPC at api.mainnet-beta.solana.com (read-only, slot 420524976)
---

# Headline

The original WP2.4 closure handover at commit `3dc34649` framed steps 2 through 6 as outstanding. Live verification this session found that steps 2, 3, and 5 were already completed by external commits before the handover doc was authored, and step 6 was completed by this session. Only step 4 (PR #233 rebase) is a genuinely outstanding action, and a clean rebase is already prepared in a local worktree awaiting an explicit force-push decision.

The original handover is preserved at commit `3dc34649` for the historical record. This corrigendum is the authoritative state record.

# What was already done before this session began

## Step 2: PR #234 merge, DONE 2026-05-17T13:40:51Z

- **State**: MERGED on `Render-Network-OS/sw4p-pro` master.
- **mergeCommit**: `fed15bba9ad45362e99f70c557954d73c14a9fc7`
- **Head SHA at merge**: `cab2af9f164ba66a43bcef79d98ca6c55f30d3d7`
- **PR title**: `feat(testnet): WP2.4 V4.1 testnet via Circle SCA (ETH Sep / Arb Sep / Unichain Sep)`
- **Verified clean**: 6 commits, all authored by `rndrntwrk <dev@rndrntwrk.com>`, no AI co-authors, no secrets, no drift-test files touched. `testnet_v41_deploys.json` schema v2 with 4 chains (baseSepolia, sepolia, arbitrumSepolia, unichainSepolia). Runtime registry `testnet.json` correctly extended by one chain (Unichain Sepolia, chain 1301, CCTP domain 10).

## Step 3: PRs #218 and #222 close, DONE 2026-05-17T13:43

- **PR #218**: CLOSED at 2026-05-17T13:43:24Z with a verbatim supersede comment citing `fed15bb`. Base Sepolia evidence row preserved in #234's schema-v2 evidence file (exact match: address `0x016731D1b719fcDA27709d9A239963B4CF50eAA8`, hardhat-direct deploy path, identical constructor args).
- **PR #222**: CLOSED at 2026-05-17T13:43:29Z with a verbatim supersede comment citing `fed15bb`. PR #222 contained zero deploy evidence (registry/script wiring only); the Unichain Sepolia registry add it carried was already incorporated into #234's basis.

## Step 5: cycle-staging rebase, DONE externally

- **Rebased branch on origin**: `staging/devnet-frontier-rebased-2026-05-17`, HEAD `6238d5a74ee21b749b92f75205f65583d361d004`
- **Original dirty branch on origin**: `staging/devnet-frontier-2026-05-16`, HEAD `1d243c624e03f1f4ff3330b941ca3aeab31820ee` (left in place for historical reference, superseded)
- **4 clean commits on the rebased branch**, all on top of post-#247 master:
  1. `c4002f8 feat(contracts): per-chain Permit2 registry sourced from Uniswap/permit2`
  2. `0171480 feat(contracts): W1 tier1/tier2/tier3-mainnet-fork registry overlays for acceptance evidence`
  3. `b5f96c0 test(contracts): Tier 3 mainnet-fork compat tests for Avalanche and Polygon mainnets`
  4. `6238d5a docs(contracts): fence tier registry files as acceptance metadata not routing config`
- **8 files / 662 insertions / 3 deletions vs origin/master**: hardhat.config.cjs, registry/README.md, registry/permit2.json, registry/tier1.json, registry/tier2.json, registry/tier3-mainnet-fork.json, test/fork/avalanche-mainnet-compat.test.cjs, test/fork/polygon-mainnet-compat.test.cjs
- **All four step-5 drop conditions held**: drift test `test/cctp_v2_address_drift.test.cjs` preserved (no phantom delete), `scripts/deploy_v4.ts` not phantom-reverted, no `ZAP_BRIDGE_V41` block in legacy `deployed_addresses.json`, no `.env.testnet` committed.
- **No PR open against the rebased branch** as of this session; the branch sits on origin as evidence preservation, not yet proposed for merge.

# What this session completed

## Step 6: 555 mint metadata, DONE this session

- **Commit**: `60037a48` on `docs/wave-g-sw4p-earn-corpus`
- **Doc**: `docs/superpowers/audits/2026-05-18-555-mint-metadata.md`
- **Probe slot**: 420524976 (Solana mainnet, `api.mainnet-beta.solana.com` JSON-RPC plus `solana` CLI cross-check)
- **Findings**:
  - decimals: **6**
  - supply (raw base units): `989859465050629`
  - supply (ui): `989859465.050629` 555
  - mint authority: **null (renounced)**
  - freeze authority: **null (renounced)**
  - token program: SPL Token (legacy), `TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA`
  - account type: `mint`, executable false
- **Phase H implication**: both authorities renounced. Token mobility cannot mint new inbound supply on Solana. Viable Phase H designs are lock-and-mint (Solana as collateralized hub) or release-after-burn from a pre-escrowed vault; designs that require Solana-side minting are off the table.

# What remains outstanding

## Step 4: PR #233 rebase, prepared but NOT pushed

- **PR**: #233 `Render-Network-OS/sw4p-pro`, OPEN, base master.
- **Branch**: `wp2.4-testnet-scp-op-poly-avax`.
- **Origin HEAD as of this session**: `ec34f71ded8986735b00d2b978fa71b66fbaa9d3` (was `4b485e94...` earlier in this session; the branch advanced once during investigation, same 2 commits, same subjects, just newer SHAs).
- **Branch state vs master**: 2 unique-evidence commits ahead, 158+ commits behind. 158-commit lag is mostly unrelated frontend/design merges (#235, #238, #240, #241, #245, etc.) plus #239 + #247 themselves.
- **Rebase output (local worktree only, not pushed)**:
  - Worktree: `/tmp/wt-233-rebase-attempt`
  - Local branch: `wp2.4-testnet-scp-op-poly-avax-rebase-attempt`
  - Post-rebase HEAD: `105059c` (tip), parent `94fa562`, on top of `981c904e` (origin/master, #247 merge)
  - **CLEAN REBASE**: no conflicts. PR #239's CCTP V2 transmitter fix on `ZapAndBridgeV41.fork.test.cjs` lives in non-overlapping hunks with #233's additions.
  - **Hardhat compile**: PASS ("Compiled 54 Solidity files successfully").
  - **CCTP V2 transmitter `0x81D40F21F12A8F0E3252Bccb954D722d4c464B64`**: present in 3 places in the rebased `ZapAndBridgeV41.fork.test.cjs` (PR #239 fix preserved).
  - **`scripts/scp_testnet_attempt.cjs`**: present (the unique 439-line empirical SCP attempt record).
  - **Author identity**: both commits authored by `rndrntwrk <dev@rndrntwrk.com>`, no `Co-Authored-By` trailers.
  - **Per-file change list vs origin/master**: `hardhat.config.cjs` +7/-1, `scripts/scp_testnet_attempt.cjs` +439/-0 (new), `test/ZapAndBridgeV41.fork.test.cjs` +32/-11.
- **Force-push command (NOT yet executed)**:
  ```
  cd /tmp/wt-233-rebase-attempt
  git push --force-with-lease=wp2.4-testnet-scp-op-poly-avax:ec34f71ded8986735b00d2b978fa71b66fbaa9d3 \
    origin wp2.4-testnet-scp-op-poly-avax-rebase-attempt:wp2.4-testnet-scp-op-poly-avax
  ```
  Lease pin guarantees the push aborts if origin advances between this verification and the push, eliminating clobber risk.
- **Authorization gate**: per the user's CLAUDE.md HARD rule ("never destructive git commands unless explicitly requested"), and per the handover's own hard boundary 9 ("No destructive git without explicit instruction"), this push waits for an explicit one-time go.

# Post-#247 master state, reverified

Re-checked on origin/master, all consistent with the handover's table:

| Surface                                       | State                                                 |
|-----------------------------------------------|-------------------------------------------------------|
| `registry/mainnet.json`                       | 6 chains: ETH, BASE, ARB, OP, AVAX, MATIC (all CCTP V2). UNI **not** present. |
| `scripts/deploy_targets_mainnet.json`         | 1 chain: UNI (CCTP V2). The script-owned delta.       |
| `scripts/deployed_addresses_v41.json`         | 7 chains, addresses match the canonical table verbatim. |
| `scripts/deploy_v4.ts` post-#239              | CCTP V2 transmitter `0x81D40F21F12A8F0E3252Bccb954D722d4c464B64` in CHAIN_META for all six mainnets + UNI block. |
| `test/cctp_v2_address_drift.test.cjs`         | Present, 42 assertions, the drift regression test from #239. |
| All seven V4.1 mainnet contracts              | INERT. No router/indexer/frontend wiring; runtime registry stayed at 6. |
| PR #221 legacy `0xaafa1e3d...`                | Closed with two poison comments forward-referencing #247. |

# Corrections to the original handover

The original handover at commit `3dc34649` carries the following errors. Read it for the underlying reasoning and reference material, but apply these corrections:

1. **"Step 2: Merge #234" is stale.** #234 was already merged at the time the handover was written (2026-05-17T13:40:51Z, mergeCommit `fed15bba`), and is now reflected on origin/master.
2. **"Step 3: Close #218 and #222" is stale.** Both PRs were closed within 3 minutes of #234's merge, with proper supersede comments citing `fed15bb`.
3. **"Step 5: Rebase cycle staging" is stale.** The rebased branch `staging/devnet-frontier-rebased-2026-05-17` exists on origin with a clean 4-commit history on post-#247 master.
4. **Branch name in step 4 was wrong.** The handover named the #233 branch `wp2.4-blocked-testnet-evidence-2026-05-13`; the actual branch is `wp2.4-testnet-scp-op-poly-avax`.
5. **"Drop the duplicate UNI-add commit" in step 4 is not applicable.** #233's two commits are pure unique evidence (empirical SCP testnet attempts + OP/AVAX mainnet fork sims). No duplicate UNI commit exists on the branch.
6. **CCTP V2 P0 amendment retraction in `2026-05-17-wp2.4-cctp-v2-p0-amendment.md` is historical.** That doc's "WP2.4-cleared retracted" framing was written pre-#239 merge; #239 merged 2026-05-17T06:51:27Z, and master has been clean ever since.

The seven canonical V4.1 mainnet addresses in the handover, the governance triple (admin / pauser / treasury), the hard contract caps (MAX_PLATFORM_FEE_BPS / AUTO_UNPAUSE_SECONDS / TIMELOCK_DELAY / globalDailyLimit), the `0xaafa1e3d...` legacy do-not-route entry, the risk register, and the glossary are all accurate and remain reference material.

# Phase H readiness

After step 4 force-push:

- All five reconciliation-queue steps are closed.
- All seven V4.1 mainnet contracts remain INERT (per-chain cutover is the next authorization gate, intentionally separate).
- 555 mint metadata is captured with both authorities renounced.

Phase H planning (NTT vs Hyperlane, with the lock-and-mint constraint forced by Solana-side authority renunciation) can begin under explicit user authorization, in a separate session. This session does not commence Phase H design work.

# Final state expected after force-push authorization

- PR #233 origin HEAD becomes the rebased tip (`105059c` or equivalent post-push SHA).
- All other PR/branch state remains as captured above.
- The receiving team posts a single closing summary referencing this corrigendum and the 555 mint metadata doc, stops, and awaits per-chain cutover authorization.

# Stop conditions reaffirmed

The 10 stop conditions in the original handover apply unchanged. The corrigendum tightens one of them: stop condition 8 ("User has not explicitly authorized the specific merge or action") now applies specifically to the force-push of `wp2.4-testnet-scp-op-poly-avax`. No other destructive action remains in queue.
